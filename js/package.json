{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "npm --workspaces run check",
    "pack:dry-run": "npm --workspaces pack --dry-run",
    "test": "npm --workspaces test --if-present"
  }
}
